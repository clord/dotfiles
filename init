#!/bin/zsh
files=(irbrc vim zshrc zshenv zprofile zsh vimrc)
wdir=`pwd`
git submodule init
git submodule update
for f in $files; do
   echo "-> " $f
   rm ~/.$f
   ln -s $wdir/$f ~/.$f
done

mkdir -p ~/tmp
touch ~/tmp/user.vim

